A responsive and visually polished Todo application built with React that allows users to add, edit, delete, search, filter, sort, and manage tasks efficiently. The app uses localStorage to persist data across sessions and includes an interactive glassmorphism-based UI for a premium user experience.
